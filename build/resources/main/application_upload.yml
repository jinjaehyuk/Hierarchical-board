spring:
  datasource:
    url: jdbc:mysql://localhost:3306/[테이블명]?serverTimezone=Asia/Seoul
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
